Archivo de bienvenida

Agrega los cambios al stage
    git add .

Agregar commit al stage
    git commit -m "Primer commit"

Subir el codigo en el repositorio remoto
    git push origin <rama>

Cambiar o crear ramas
    git checkout <rama>
    git branch <rama>


Como podemos correr una imagen de Nginx configurando el  puerto de mi host 
el numero 9000.

Crear un volumen, y luego asignarlo a la ruta /x de un contenedor wordpress

copiar un fichero dentro de un contenedor de Nginx.